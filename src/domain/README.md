Pure business logic for Crumb. No React, no Dexie, no DOM — just typed functions and data
that take inputs and return outputs. This is what unit tests in this project mostly target.

Examples of what belongs here once feature work starts: hydration math, feeding ratio
calculations, fermentation time models, schedule back-calculation.
